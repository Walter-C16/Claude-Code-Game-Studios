## Smoke Check Report
**Date**: 2026-04-10
**Sprint**: Sprint 1 — Foundation Layer
**Engine**: Godot 4.6
**QA Plan**: production/qa/qa-plan-sprint-1-2026-04-10.md
**Argument**: quick (re-check after bugfixes)

---

### Automated Tests

**Status**: NOT RUN — Godot binary not on PATH

Manual confirmation required: run tests from Godot editor (GdUnit4 panel) or CI.

---

### Test Coverage

Coverage scan skipped — run `/smoke-check sprint` for full coverage analysis.

---

### Manual Smoke Checks

- [x] Game launches to Splash screen without crash — **PASS**
- [x] New Game navigates to Hub screen with fade transition — **PASS**
- [-] Save / load — skipped (quick mode)
- [-] Performance — skipped (quick mode)

---

### Bugfixes Applied This Session

1. **class_name autoload conflict** — Removed `class_name` from all 6 autoload scripts. Updated ~20 test files to use `preload().new()`.
2. **Legacy API migration** — Updated 7 scene/system scripts to use new GameStore API (`set_flag()`, `set_met()`, `get_gold()`, `has_flag()`, `get_last_captain_id()`).
3. **Scene path prefix** — Changed all `SCENE_PATHS` from `res://src/scenes/` to `res://scenes/` (project.godot lives inside `src/`).
4. **Legacy singleton references** — `SaveSystem` → `SaveManager`, `GameStore.reset_state()` → `_initialize_defaults()`, string paths → `SceneId` enums.

---

### Missing Test Evidence

All Logic and Integration stories have test coverage (verified in prior full smoke check).

---

### Verdict: PASS WITH WARNINGS

- [x] Batch 1 (core stability): PASS — game launches clean
- [x] Batch 2 (sprint mechanics): PASS — scene navigation works
- Automated tests: NOT RUN (confirm from editor/CI)
- Coverage: skipped (quick mode)

Build is ready for manual QA pending automated test confirmation.
